消息摘要

包括MD, SHA 和 MAC 三个系列，常用于验证数据的完整性，数字签名核心算法

MD
    1. 产生一个128位的信息摘要，转换成32位的字符串；每4位二进制转换1位十六进制
SHA
    1. SHA-1是40位十六进制字符串
    2. SHA-256是64位十六进制字符串
    3. SHA-384是384位十六进制字符串
    4. SHA-512是512位十六进制字符串
    
MAC ：兼容了MD和SHA的特性，并在此基础上加入了密钥支持
    1. MD系列有HmacMD2，HmacMD4和HmacMD5
    2. SHA系列有HmacSHA1，HmacSHA224,HmacSHA256,HmacSHA348,HmacSHA512
    
crc-32
    1. 算是古老的数据完整型验证算法