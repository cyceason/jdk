jdk 1.7 新特性

测试不出来的
	自定义自动关闭类
	新增一些取环境信息的工具方法 ： System 有关的
	Boolean类型反转，空指针安全,参与位运算
	两个char间的equals ： 	Character.equalsIgnoreCase(char ch1, char ch2)
	安全的加减乘除 ： Math 工具类
	对Java集合（Collections）的增强支持 : 即类似数组的读取方式
	Switch中可用String
	jdk7之前，你必须用try{}finally{}在try内使用资源，在finally中关闭资源，不管try中的代码是否正常退出或者异常退出。jdk7之后，你可以不必要写finally语句来关闭资源，只要你在try()的括号内部定义要使用的资源